# No dependency may execute code during install. Lifecycle scripts (preinstall,
# install, postinstall) are the usual delivery mechanism for a compromised
# package, and nothing this project depends on needs to compile or generate
# anything, so there is no reason to allow them.
ignore-scripts=true

# Fail rather than silently reach for a package that is not in the lockfile.
audit=true
fund=false
